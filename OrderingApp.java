
// import utils.Utils;

/**
 * OrderingApp
 */
public class OrderingApp {
    public static void main(String[] args) {
        // Selection Sort, test one at a time!
        // Utils.testSorting(100000, "selection");
        // Utils.testSorting(200000, "selection");
        // Utils.testSorting(500000, "selection");
        // Utils.testSorting(1000000, "selection");

        // Insertion Sort, test one at a time!
        // Utils.testSorting(100000, "insertion");
        // Utils.testSorting(200000, "insertion");
        // Utils.testSorting(500000, "insertion");
        // Utils.testSorting(1000000, "insertion");

        // Quick Sort, test one at a time!
        // Utils.testSorting(100000, "quick");
        // Utils.testSorting(200000, "quick");
        // Utils.testSorting(500000, "quick");
        // Utils.testSorting(1000000, "quick");
    }
}
